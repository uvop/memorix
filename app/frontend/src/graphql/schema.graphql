schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Mutation {
  echo(message: String!): Boolean!
}

type Query {
  test: Boolean!
}

type Subscription {
  listenToEchoes: String!
}
